<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C.M.D. - Settings</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=Playfair+Display:wght@600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/CM_UserSettings.css">
</head>
<body>

    <nav class="navbar">
        <a href="/" class="brand"><span class="brand-light">C.M.</span><span class="brand-accent">D.</span></a>
        <div class="nav-links">
            <a href="/events">Explore</a>
            <a href="/posts/create">Create</a>
            <a href="/users/profile">Profile</a>
            <a href="/about">About Us</a>
        </div>
        <div class="nav-right">
            <% if (currentUser) { %>
                <span><%= currentUsername %></span>
                <form action="/logout" method="POST" style="display:inline">
                    <button type="submit" style="margin-left:1rem;color:hsl(20,90%,55%);background:none;border:none;cursor:pointer;font:inherit;">Log Out</button>
                </form>
            <% } else { %>
                <a href="/login" style="color:hsl(20,90%,55%);text-decoration:none;">Sign In</a>
            <% } %>
        </div>
    </nav>

    <% if (success && success.length) { %><div class="flash success"><%= success %></div><% } %>
    <% if (error && error.length) { %><div class="flash error"><%= error %></div><% } %>

    <div class="settings-layout">
        <aside class="settings-sidebar">
            <button class="nav-item active" onclick="setSection(this, 'profile')">Profile</button>
            <button class="nav-item" onclick="setSection(this, 'interests')">Interests</button>
            <button class="nav-item" onclick="setSection(this, 'location')">Location</button>
        </aside>

        <div class="settings-content">

            <form action="/users/settings" method="POST" id="settingsForm">

                <div class="section" id="section-profile">
                    <h2 class="section-title">Profile</h2>
                    <div class="field">
                        <label class="field-label" for="settingName">Display Name</label>
                        <input type="text" id="settingName" name="name" class="field-input"
                               value="<%= user.name %>" required />
                    </div>
                    <div class="save-bar">
                        <button type="submit" class="btn-save">Save Changes</button>
                        <a href="/users/profile" class="btn-discard">Discard</a>
                    </div>
                </div>

                <div class="section" id="section-interests" style="display:none;">
                    <h2 class="section-title">Interests</h2>
                    <p class="section-desc">Enter your interests as comma-separated values.</p>
                    <div class="field">
                        <label class="field-label" for="settingInterests">Interests</label>
                        <input type="text" id="settingInterests" name="interests"
                               class="field-input"
                               value="<%= (user.interests || []).join(', ') %>"
                               placeholder="e.g. Outdoors, Music, Tech" />
                        <small>Separate each interest with a comma.</small>
                    </div>
                    <div class="save-bar">
                        <button type="submit" class="btn-save">Save Changes</button>
                        <a href="/users/profile" class="btn-discard">Discard</a>
                    </div>
                </div>

                <div class="section" id="section-location" style="display:none;">
                    <h2 class="section-title">Location</h2>
                    <div class="field">
                        <label class="field-label" for="settingLocation">City / Neighborhood</label>
                        <input type="text" id="settingLocation" name="location" class="field-input"
                               value="<%= user.location || '' %>"
                               placeholder="e.g. Capitol Hill, Seattle" />
                    </div>
                    <div class="save-bar">
                        <button type="submit" class="btn-save">Save Changes</button>
                        <a href="/users/profile" class="btn-discard">Discard</a>
                    </div>
                </div>

            </form>
        </div>
    </div>

    <footer>
        <div class="container">
            <span class="logo">C.M.D.</span>
            <p>Connect. Meet. Discover. &copy; 2026</p>
        </div>
    </footer>

    <script src="/js/CM_UserSettings.js"></script>
</body>
</html>
